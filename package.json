{
  "name": "codex-config",
  "version": "0.144.1",
  "description": "Keep Codex config.toml current for the GPT-5.6 model family without overwriting unrelated settings.",
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@10.33.4",
  "bin": {
    "codex-config": "dist/cli.js"
  },
  "files": [
    "dist",
    "config.toml.template",
    "config.schema.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx src/cli.ts",
    "sync:codex": "node scripts/sync-codex.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "commander": "^14.0.2",
    "smol-toml": "^1.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "keywords": [
    "codex",
    "config",
    "toml",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PsychArch/codex-config.git"
  }
}
